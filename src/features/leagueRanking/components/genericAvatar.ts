/** Moved to `src/lib/playerPortrait.ts` so the ball and the board share one definition.
    Re-exported here because this module name is what the shield card and its tests import. */
export { genericAvatarUrl } from '@/lib/playerPortrait';
